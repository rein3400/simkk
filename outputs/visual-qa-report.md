# Visual QA Report — 2026-06-06

Generated as part of the final handoff package for SIM-KK.

## 1. Build status — `apps/web` (Vue 3 + Vite)

Command: `cd apps/web && npm run build`

```
> sim-kk-web@0.2.0 build
> vue-tsc --noEmit && vite build

src/App.vue(74,24): error TS7053: Element implicitly has an 'any' type because expression of type 'any' can't be used to index type 'Record<Role, RoleProfile>'.
src/views/LoginView.vue(13,7): error TS2741: Property 'Admin' is missing in type '{ Kasir: string; Terapis: string; Gudang: string; Manajer: string; }' but required in type 'Record<Role, string>'.
```

Result: **FAIL** — `vue-tsc --noEmit` blocks the build with 2 TypeScript errors.
The Vite bundler step never executes. Both errors are pre-existing and unrelated
to the visual QA scope; the offending files are `src/App.vue` and
`src/views/LoginView.vue`.

## 2. Test status — `apps/api` (Laravel + PHPUnit)

Command: `cd apps/api && php artisan test`

- **Tests run:** 25
- **Passed:** 24
- **Failed:** 1
- **Assertions:** 55
- **Duration:** ~1.04s

Failing test (pre-existing, not introduced by visual QA work):

- `Tests\Unit\FifoStockTest::test_fifo_throws_on_insufficient_stock`
  - Expected: `RuntimeException`
  - Actual: `Illuminate\Validation\ValidationException` (message: "Stok tidak cukup untuk produk #1 (butuh 5, tersedia 2).")
  - File: `apps/api/tests/Unit/FifoStockTest.php:38`
  - Diagnosis: `TransaksiService` is dispatching the error through Laravel's
    validator instead of throwing the raw `RuntimeException` the test asserts on.
    Logic is correct — only the exception type is mismatched.

## 3. Visual screenshots

9 PNGs regenerated by `outputs/sim-kk-ui-previews/screenshot.cjs` at 1440x900
viewport. All artifacts are under
`D:\users\stefa\project\sim-kk\outputs\sim-kk-ui-previews\assets\`.

| Screen                  | File                                          | Size (bytes) |
|-------------------------|-----------------------------------------------|--------------|
| Login                   | `assets/login.png`                            | 477,221      |
| POS                     | `assets/pos.png`                              | 396,469      |
| Rekam Medis             | `assets/rekam-medis.png`                      | 78,420       |
| Gudang                  | `assets/gudang.png`                           | 90,658       |
| Laporan (overview)      | `assets/laporan.png`                          | 83,603       |
| Laporan Arus Kas        | `assets/laporan-arus-kas.png`                 | 78,268       |
| Laporan Stok Komisi     | `assets/laporan-stok-komisi.png`              | 37,817       |
| Laporan Daily           | `assets/laporan-daily.png`                    | 62,490       |
| Laporan Inventory Moves | `assets/laporan-inventory-movements.png`      | 70,209       |

Screenshot index: see `outputs/sim-kk-ui-previews/SUMMARY.md`.

## 4. Known issues (pre-existing, carried into handoff)

1. **`vue-tsc` blocks `npm run build`** — two typing errors in `App.vue` and
   `LoginView.vue` (missing `Admin` role in the `Record<Role, string>` lookup
   and an untyped index access). Need a 5-minute fix to satisfy strict mode.
2. **FIFO test expects the wrong exception class** — `FifoStockTest` asserts
   `RuntimeException` but `TransaksiService` now raises
   `ValidationException`. Either revert the service to throw raw, or update
   the test to expect the validator exception.
3. **Static preview is not wired to the API** — the 9 HTML previews ship
   fixture data; they are not a substitute for an end-to-end Playwright run
   against `apps/api`. The smoke suite under `apps/web/tests` should be the
   final gate before client demo.
4. **No automated visual regression baseline** — file-size diffs in section 3
   are the only QA signal. Pixel-diff tooling (Playwright `toHaveScreenshot`
   with a checked-in baseline) is recommended for the next iteration.

## 5. Verification evidence

- `node screenshot.cjs` exit code 0; all 9 PNGs written.
- `php artisan test` exit code 1; 24/25 pass; failing test recorded above.
- `npm run build` exit code 2; both TS errors captured verbatim.

## 6. Next actions

- [ ] Fix the two `vue-tsc` errors so `npm run build` exits 0.
- [ ] Reconcile `FifoStockTest::test_fifo_throws_on_insufficient_stock` with
      the actual `ValidationException` thrown by `TransaksiService`.
- [ ] Add a Playwright visual-regression baseline for the 9 screens.
- [ ] Re-run this report and commit the green version.

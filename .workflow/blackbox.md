[
  {
    "id": "BB-1",
    "title": "POST /api/transactions/pay returns 500 RuntimeException with full stack trace when stock insufficient (should be 422/409)",
    "severity": "High",
    "module": "Transaksi/Inventory",
    "file": "(blackbox — no source access; stack points to apps/api/app/Services/TransaksiService.php:147)",
    "lines": "N/A (observed in error.trace)",
    "repro": "POST /api/transactions/pay with items[0].serviceId=8 (Calming Toner, stock=7) and qty=10 — 5 parallel calls. All return 500 RuntimeException. Body: {\"message\":\"Stok FIFO tidak cukup untuk produk #3 (butuh 10, tersedia 7).\",\"exception\":\"RuntimeException\",\"file\":\"D:\\\\Users\\\\stefa\\\\Project\\\\SIM-KK\\\\apps\\\\api\\\\app\\\\Services\\\\TransaksiService.php\",\"line\":147,... full Laravel stack trace}",
    "expected": "422 (validation) or 409 (conflict) with the same localized message, no exception class/file/line/stacktrace leak",
    "actual": "500 with full file path, line number, framework class chain, and a raw PHP RuntimeException class. Leaks internal architecture (Service layer path). CORS /api/* with -F: shows that file path includes developer username. DoS/intel leak + bad UX.",
    "fix": "Wrap the stock-check branch in try/catch and return 422 with `errors.stock` (or 409 with `code: STOCK_INSUFFICIENT`). Disable debug stacktrace in non-local envs (APP_DEBUG=false in .env).",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-2",
    "title": "Photo upload accepts .php, .exe, .svg, .txt, and path-traversal filenames (no extension validation)",
    "severity": "Critical",
    "module": "RekamMedis (Patient photos)",
    "file": "(blackbox — no source access)",
    "lines": "N/A",
    "repro": "POST /api/patients/1/photos (terapis token) JSON: {\"label\":\"Before\",\"filename\":\"shell.php\",\"content\":\"<base64 of <?php echo 'owned'; ?>\"} -> 201 Created. objectRef=clinical/RM-2026-0018/{uuid}-shell.php. Same result for filename: shell.exe, x.svg, notes.txt. Path-traversal filename '../../../etc/passwd' -> 201, sanitized to `..-..-..-etc-passwd` on disk but still accepted on input.",
    "expected": "422 with extension not in allowlist (jpg,jpeg,png,webp,heic). Path traversal segments rejected or stripped before storage.",
    "actual": "201 for all executable / arbitrary extensions. Confirms RCE risk identical to whitebox finding WB-2 but observed from blackbox side — verifies the bug is exploitable end-to-end without source.",
    "fix": "Add `Rule::in(['jpg','jpeg','png','webp'])` on filename. Reject filenames containing `..`, `/`, `\\`. Magic-byte sniff decoded content.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-3",
    "title": "Stored XSS in paymentMethod reflected via /api/bootstrap",
    "severity": "Medium",
    "module": "Transaksi/Bootstrap",
    "file": "(blackbox — no source access)",
    "lines": "N/A",
    "repro": "TRX-260604-008 observed in current bootstrap: paymentMethod = '<script>alert(1)</script>'. The field was stored earlier and is served raw to any client calling GET /api/bootstrap (all roles). Confirmed by parsing the JSON returned for current state.",
    "expected": "Either an allowlist (Tunai/QRIS/EDC/Transfer) or htmlspecialchars at bootstrap serialization boundary.",
    "actual": "Bootstrap returns paymentMethod verbatim. Any admin/owner UI rendering this list (the dashboard does) executes the script. Confirms whitebox finding WB-3 from outside the codebase.",
    "fix": "Allowlist enum in validation. Escape in BootstrapController when mapping `metode_bayar` to `paymentMethod`.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-4",
    "title": "Discount field silently coerced to 0 for any non-integer or out-of-range value",
    "severity": "High",
    "module": "Transaksi",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "POST /api/transactions/pay with `discount` field as: \"50000\" (string), 50000.5 (float), 285000 (== subtotal), 285001 (> subtotal), -100000 (negative), 99999999 (huge). All return 201 with `discount: 0` in the response. The total is always subtotal. No 422 returned, no warning.",
    "expected": "422 with 'discount must be an integer 0..subtotal' for invalid values. Successful discount must be stored as-is, not coerced to 0.",
    "actual": "Silent normalization. A kasir cannot apply a valid 50000 discount because the system silently drops it. The total invoice amount will be wrong. Likely a `cast to int` in the controller/model with a default that swallows the cast failure.",
    "fix": "Validate: `integer|min:0|lte:subtotal`. Reject float/string with 422. Use proper `IntegerType` cast in the resource.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-5",
    "title": "No qty sanity cap — qty=99999 produces 28.5 billion transaction",
    "severity": "High",
    "module": "Transaksi",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "POST /api/transactions/pay items=[{serviceId:1, qty:99999}] -> 201. subtotal=28,499,715,000 (28.5 milyar), total same, commission=3,419,965,800. Commission alone exceeds all seed commission sums. No upper bound enforced.",
    "expected": "422 'qty exceeds reasonable limit' or a per-cart cap (e.g. qty<=100, subtotal<=100jt).",
    "actual": "28 billion transaction committed. Receipt recorded, cash_ledger debited, commission paid. The finance report will show garbage totals.",
    "fix": "Add `qty|max:100` (or service-specific). Add `subtotal|max:100000000` at controller level. Defend with a DB CHECK constraint as last resort.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-6",
    "title": "No duplicate item dedupe in cart — same serviceId twice counted twice",
    "severity": "Medium",
    "module": "Transaksi",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "POST /api/transactions/pay items=[{serviceId:1,qty:1},{serviceId:1,qty:1}] -> 201. subtotal=570000 (2x 285000). Each line is treated as a separate order line. No 422, no merge.",
    "expected": "Either reject duplicates with 422 'duplicate service in cart' OR merge qty.",
    "actual": "Accepted as-is. Allows a kasir to accidentally double-charge or double-decrement stock. Stock was decremented twice (2 units from 1 batch line) — confirmed by stock delta (9->7 from 3 Calming Toner pays each with 1 unit; an extra dup-item pay would have decremented 1 more, but was masked here).",
    "fix": "Either merge in controller (`collect($items)->groupBy('serviceId')->map(fn($g)=>['serviceId'=>..., 'qty'=>$g->sum('qty')])`) or reject with 422.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-7",
    "title": "Duplicate `kode_batch` in /api/inventory/purchases silently accepted",
    "severity": "High",
    "module": "Inventory",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "POST /api/inventory/purchases (gudang) with produk_id=1, kode_batch='BS-0526-B' (already exists), qty=5, hpp=90000. Returns 201 with `newBatch.code=BS-0526-B`. Now two batches with the same code but different HPP. FIFO logic ambiguous — firstOut flag may now be wrong.",
    "expected": "422 'kode_batch already exists for this product' OR auto-merge to existing batch (qty +=, weighted HPP).",
    "actual": "Accepted silently. Audit trail broken. FIFO picking may deplete the wrong HPP record. Total stock counted twice for the same code.",
    "fix": "Add unique constraint on (produk_id, kode_batch) and return 422 on conflict. Or implement explicit merge endpoint.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-8",
    "title": "Path traversal in `kode_batch` accepted; storage layer sanitizes but no validation upstream",
    "severity": "Medium",
    "module": "Inventory",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "POST /api/inventory/purchases with kode_batch='../../etc/passwd' -> 201. Stored as `..\/..\/etc\/passwd` in the response. (Storage layer turned slashes into escaped slashes — partial mitigation.) No 422 on input.",
    "expected": "422 'invalid kode_batch format'.",
    "actual": "Acceptance. A kasir/gudang typing `../` in a batch code gets it committed. Reports showing batch codes may also have unexpected characters, breaking parsers.",
    "fix": "Validate: `regex:/^[A-Za-z0-9._-]+$/` (alphanumeric + dot + dash + underscore). Reject `..` segments explicitly.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-9",
    "title": "Race condition: 5x parallel /api/transactions/pay for same cart — 1 wins, 4 return 500 (no idempotency, no graceful fail)",
    "severity": "High",
    "module": "Transaksi/Concurrency",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "5 parallel POSTs with same payload (Calming Toner x5, stock=7) — first call gets 201, subsequent 4 get 500 RuntimeException. Sequential TRX ids 026, 027,... assigned (atomic counter OK), but stock check is not lock-protected. The first pay decrements 5 leaving 2; calls 2-5 each try to decrement 5 and 4 of them fail with the same 500 stock message.",
    "expected": "Either DB-level row lock + retry, OR queue-based serialization, OR at minimum 422/409 instead of 500.",
    "actual": "Half the requests fail with 500. The 'one wins, rest fail' pattern is correct for stock contention, but the status code is wrong. With 4 users at peak, 80% of POS requests would 500.",
    "fix": "Use SELECT ... FOR UPDATE on batch rows in decrementStock(); retry once on deadlock; map final failure to 409.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-10",
    "title": "No idempotency key on /api/transactions/pay — client retries create duplicate TRX",
    "severity": "High",
    "module": "Transaksi",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "5 sequential POSTs with same payload, all return 201 (TRX-021..025). Each call decrements stock, appends to cash_ledger, records a new transaction. There is no `Idempotency-Key` header support, no client-supplied request id, no payload hash dedup.",
    "expected": "Either accept `Idempotency-Key` header and dedupe (Stripe-style) or use a `request_id` field. Replay of same payload within window returns the same TRX id.",
    "actual": "5 different TRX, 5x stock decrements (5x same product = 5 units), 5x commission. A flaky network retrying the request will multiply the financial impact by N.",
    "fix": "Add `Idempotency-Key` header support. Persist (key, response) for 24h, return cached response on replay. Critical for payment endpoints.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-11",
    "title": "/api/bootstrap supports GET only; spec/mission says POST",
    "severity": "Low",
    "module": "Bootstrap",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "POST /api/bootstrap with any valid token -> 405 MethodNotAllowedHttpException. GET /api/bootstrap -> 200.",
    "expected": "Either accept POST (per spec/mission text) or document GET as the canonical verb. 405 should be 404 if POST is not supported, with Allow header.",
    "actual": "405 with verbose exception (file/line) returned. Spec said POST; reality is GET. Two bugs: contract violation + verbose 405 page.",
    "fix": "Add POST route alias OR change spec. Suppress debug stacktrace on 4xx.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-12",
    "title": "Login requires undocumented `role` field; omitting returns 500 stack trace",
    "severity": "High",
    "module": "Auth",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "POST /api/login body={\"username\":\"kasir\",\"password\":\"simkk-2026\"} (no `role` key) -> 500 ErrorException: 'Undefined array key \"role\"' at AuthController.php:23 with full trace. With `role:'Kasir'` added -> 200.",
    "expected": "422 'level or role required'.",
    "actual": "500 with file/line/trace. Same bug as whitebox WB-1, observed from blackbox side — meaning the frontend or any third-party caller hitting this endpoint without the `role` key will crash. Cross-confirmed.",
    "fix": "Make rule `required` not `sometimes|required`. Use `data_get()` for safe access.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-13",
    "title": "Receipt id format is 'RCPT-TRX-...' (compound) — spec expected plain 'RCPT-...'",
    "severity": "Low",
    "module": "Transaksi",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "Every successful /api/transactions/pay returns receipt.id = 'RCPT-TRX-260604-NNN'. The spec's example was plain 'RCPT-...'.",
    "expected": "Either `RCPT-260604-NNN` (date+seq) or some other short id. The current format leaks the transaction id into the receipt id (coupling) and is 22 chars long.",
    "actual": "Coupling between receipt and TRX id. If TRX gets a 'void' flag later, receipt id cannot be reused. UX has to display a long string.",
    "fix": "Generate receipt id from its own counter. Pattern: `RCPT-YYMMDD-NNNN`.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-14",
    "title": "catatan field in /api/transactions/pay accepts 1MB+ strings with no length cap",
    "severity": "Medium",
    "module": "Transaksi",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "POST /api/transactions/pay with catatan=1,000,000 'X' chars -> 201. POST /api/patients/1/treatments with catatan=1,000,000 chars -> 201. Storage confirmed: bootstrap's `notes` field for the 1MB treatment shows notes-len=1000000.",
    "expected": "422 'catatan max 1000 chars'.",
    "actual": "No length cap. DoS via 1MB notes; DB bloat; UI will lag rendering the notes. PHP post_max_size (8MB) is the only limit — a 7MB catatan would still pass.",
    "fix": "Add `catatan|max:1000` and `notes|max:1000` in the FormRequest.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-15",
    "title": "Finance PDF report (manajer-only) is structurally valid but renders NO content (8-byte FlateDecode stream)",
    "severity": "High",
    "module": "Reports/Finance",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "GET /api/reports/finance/export (manajer token) -> 200, 919 bytes, header '%PDF-1.7'. Inspected raw: `/Producer (dompdf 3.1.5 + CPDF)`, `/CreationDate (D:20260604152427+00'00')`, `/Length 8` for the page content stream. pdftotext extracts 0 chars. The page is blank.",
    "expected": "PDF with 'Laporan Arus Kas' title, table of TRX, debit/kredit/saldo columns. Sum of all transactions should appear.",
    "actual": "Empty PDF. The bootstrap's `reports.finance.rows` shows data is being computed correctly, but the PDF export does not include it. The manajer opens the report and sees a blank page. /api/reports/stock/export and /api/reports/commission/export produce valid XLSX with content — only the PDF is broken.",
    "fix": "Inspect FinanceController PDF render path. Either pass rows into the view template, or dompdf needs the rows in `loadView('pdf.finance', [...])`. The 8-byte compressed stream is the giveaway — the data array is empty.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-16",
    "title": "Commission XLSX export uses sheet name 'Worksheet' (default) and 'Grand Total (Rp)' header (not 'Take Home Pay' from spec)",
    "severity": "Low",
    "module": "Reports/Commission",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "Unzipped stock.xlsx and commission.xlsx. Sheet names: 'Worksheet' (both). Commission headers: 'ID Pegawai', 'Nama Terapis', 'Jumlah Tindakan', 'Total Komisi (Rp)', 'Gaji Pokok (Rp)', 'Grand Total (Rp)'. Stock headers: 'Produk', 'Stok', 'Batch', 'HPP (Rp)'.",
    "expected": "Meaningful sheet names (e.g. 'Komisi Terapis', 'Stok FIFO' or 'Laporan Stok'). 'Take Home Pay' or matching the spec.",
    "actual": "Sheet name 'Worksheet' is the phpoffice default — never set. Header naming deviates from spec text.",
    "fix": "Use `$sheet->setTitle('Komisi Terapis')` (and 'Stok FIFO' for stock). Reconcile header naming with the spec terms.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-17",
    "title": "404 NotFoundHttpException includes full stack trace and absolute file paths on every non-existent route",
    "severity": "Medium",
    "module": "Routing/Errors",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "GET /api/transactions/999 -> 404 with message 'No query results for model [App\\Models\\Pasien] 99' (this is a wrong model — the URL is /api/transactions/999 but the error talks about Pasien — there's an Eloquent implicit-binding bug too). GET /api/services -> 404 with stack trace from AbstractRouteCollection. GET /api/anything -> 404 stack trace.",
    "expected": "404 with `{\"message\":\"Not found\"}` for unknown routes; 404 `{\"message\":\"Transaction not found\"}` for /api/transactions/{id} with non-existent id; 422/404 for invalid UUID on Eloquent binding.",
    "actual": "Three problems in one: (1) every 404 leaks file paths and the exception class chain; (2) /api/transactions/999 returns Pasien error not Transaksi — model is mismatched; (3) /api/services, /api/therapists etc. all 404 — no read endpoints exist for any of these resources, forcing the frontend to always go through /api/bootstrap.",
    "fix": "Disable APP_DEBUG in non-local env. Add proper resource controllers for /api/services, /api/therapists, /api/transactions/{id}, /api/inventory/{id}. Fix the implicit binding in the wrong controller.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-18",
    "title": "CORS: Access-Control-Allow-Origin: * on authenticated endpoints (CSRF / cross-origin attack surface)",
    "severity": "Medium",
    "module": "Security/CORS",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "OPTIONS /api/transactions/pay -H 'Origin: http://evil.com' -i -> 200 with `Access-Control-Allow-Origin: *`. Same for all /api/* endpoints (Laravel default when HandleCors is permissive and Access-Control-Allow-Credentials is not set).",
    "expected": "Echo the request Origin only if it's in the allowlist; OR fixed origin (e.g. https://app.simkk.local); OR `*` only for non-authenticated routes.",
    "actual": "Wildcard. Any browser-side code on any origin can POST /api/transactions/pay. Tokens in Authorization header are same-origin-protected only if the SPA lives on the same origin; if not, attacker site can read the response (no wildcard exception here because no credentials). However, the lack of any Origin restriction is still wrong for an internal business app.",
    "fix": "Configure `cors.allowed_origins` in config to specific domains. Reject mismatched Origin on state-changing methods with 403.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-19",
    "title": "Cash ledger bootstrap inconsistency: bootstrap endpoint does not return cash_ledger total but reports.finance.rows saldos exceed sum of all transaksi.total",
    "severity": "Medium",
    "module": "Reports/Finance",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "After all my 30+ pays, last finance row saldo is 'Rp12.305.000' (TRX-260604-026). Sum of all my TRX-260604-* total = 11,730,000 (from `verify_total.sh`). Including seed: seed=1,095,000 (TRX-2505-031+032), and TRX-260604-026 alone is 725,000. 11,730,000+1,095,000 = 12,825,000 vs saldo 12,305,000. Off by 520,000.",
    "expected": "Saldo = sum of all TRX-260604-*.total + seed totals exactly. Off by Rp520.000 means one row is missing or the running total has a bug.",
    "actual": "Bootstrap's `reports.finance.rows` is computed independently of the actual transaksi table. Likely uses a separate cash_ledger table that drifted. The mismatch is silent — no integrity check.",
    "fix": "Make finance report derive from Transaksi::where('status','Lunas')->sum('total'). Or add a daily reconciliation that compares cash_ledger balance to Transaksi sum and surfaces the diff.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-20",
    "title": "Form-urlencoded body to /api/transactions/pay is accepted and creates real transactions (Content-Type confusion)",
    "severity": "Medium",
    "module": "Transaksi",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "curl -X POST -H 'Authorization: Bearer $TK' --data-urlencode 'pasien_id=1' --data-urlencode 'terapis_id=1' --data-urlencode 'items[0][serviceId]=1' http://127.0.0.1:8000/api/transactions/pay -> 201, real TRX created (TRX-260604-038).",
    "expected": "415 Unsupported Media Type. Only application/json should be accepted for JSON-shaped endpoints.",
    "actual": "Laravel's request layer silently re-parses form body into Request input, so the controller sees the same array. This is a footgun: a misconfigured client (or a CSRF form-based attacker page) can submit a transaction as application/x-www-form-urlencoded and bypass any client-side JSON-only guard.",
    "fix": "In the FormRequest, assert `request()->isJson()` or `Content-Type: application/json` header. Reject otherwise with 415.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-21",
    "title": "SQL injection probes in serviceId and other fields — properly rejected by integer validation (no finding)",
    "severity": "Info",
    "module": "Transaksi/Auth",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "items[0].serviceId='1 OR 1=1' -> 422 'serviceId must be an integer'. items[0].serviceId='1; DROP TABLE transaksi;--' -> 422 same. username='1=1' -> 401 from auth, not vulnerable. All probes returned validation errors, not SQL.",
    "expected": "Reject non-integer with 422.",
    "actual": "Pass. Strong-types validation in FormRequest prevents obvious SQLi at the input boundary.",
    "fix": "None.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-22",
    "title": "Stock decrement correctness: qty*N pays correctly decrement across FIFO batches (positive finding)",
    "severity": "Info",
    "module": "Transaksi/Inventory",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "Started with Calming Toner totalStock=9 (1 batch CT-0326-C qty=9). Three pays with serviceId=8 (qty=1 each) reduced it to 6. Then race test (1 batch left, 7 stock) with 5x qty=5 parallel: first won (725k total, -5 stock leaving 2), rest 500'd. Net stock math correct under serial flow. Barrier Serum went 34 -> 33 (lost 1 to a 0426-A which had been removed earlier) -> 16 -> 13 -> 12 etc — all matches the test actions.",
    "expected": "Decrement stock atomically per request.",
    "actual": "Pass under serial. Race protection is the only weak point (covered by BB-9).",
    "fix": "None for serial path.",
    "status": "CONFIRMED"
  },
  {
    "id": "BB-23",
    "title": "Commission math correct for paket (Paket Acne 3x 760000 * 0.11 = 83600; Paket Bridal Glow 1450000 * 0.13 = 188500)",
    "severity": "Info",
    "module": "Transaksi/Reports",
    "file": "(blackbox)",
    "lines": "N/A",
    "repro": "Manajer pay serviceId=9 -> commission: 83600. Manajer pay serviceId=10 -> commission: 188500. Kasir pay serviceId=1,6,8 -> commission: 34200 + 10750 + 5800 = 50750 (matches round(285000*0.12 + 215000*0.05 + 145000*0.04)).",
    "expected": "komisi = round(price * commissionRate).",
    "actual": "Pass.",
    "fix": "None.",
    "status": "CONFIRMED"
  }
]

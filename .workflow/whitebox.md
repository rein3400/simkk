[
  {
    "id": "WB-1",
    "title": "Login 500 when level/role omitted (Undefined array key 'role')",
    "severity": "High",
    "module": "Auth",
    "file": "apps/api/app/Http/Controllers/Api/AuthController.php",
    "lines": "L16-L27",
    "repro": "POST /api/login body={\"username\":\"x\",\"password\":\"y\"} -> 500 ErrorException",
    "expected": "422 validation error: 'level or role required'",
    "actual": "500 with full stack trace: 'Undefined array key \"role\"' at line 23 (operator ?? cannot fallback to absent key, only null)",
    "fix": "Replace `$validated['level'] ?? $validated['role']` with `data_get($validated, 'level', data_get($validated, 'role'))` OR make the rule `required` not `sometimes|required` so missing key fails validation before reaching line 23.",
    "status": "CONFIRMED"
  },
  {
    "id": "WB-2",
    "title": "Photo upload stores raw .php file in public disk -> RCE when /storage/ is served by PHP",
    "severity": "Critical",
    "module": "Storage/RekamMedis",
    "file": "apps/api/app/Services/StorageService.php + RekamMedisController.php",
    "lines": "StorageService.php L10-L26; RekamMedisController.php L42-L50",
    "repro": "POST /api/patients/1/photos body={\"label\":\"Before\",\"filename\":\"shell.php\",\"content\":\"<?php phpinfo(); ?>\"} -> 201; object_ref=clinical/RM-2026-0018/{uuid}-shell.php; GET /storage/clinical/.../shell.php -> 200 text/html with rendered phpinfo() (server EXECUTED the file).",
    "expected": "Reject .php extension OR strip executable extensions OR store on non-public disk OR deny PHP execution in storage path via .htaccess",
    "actual": "File is stored verbatim with original .php extension on the 'public' disk which is symlinked to public/storage and PHP-executed by the dev server. Confirmed via curl: response is rendered phpinfo() HTML, not raw text.",
    "fix": "(1) Add `in:jpg,jpeg,png,webp` validation on `filename` extension. (2) Use `disk('local')` not `disk('public')` and serve via authenticated download route. (3) Add storage/.htaccess with `php_flag engine off` as defense in depth. (4) Magic-byte sniff the decoded content before storing.",
    "status": "CONFIRMED"
  },
  {
    "id": "WB-3",
    "title": "metode_bayar accepts raw HTML/JS and round-trips through /api/bootstrap (stored XSS)",
    "severity": "Medium",
    "module": "Transaksi",
    "file": "apps/api/app/Http/Controllers/Api/TransaksiController.php + BootstrapController.php",
    "lines": "TransaksiController.php L21 (max:32); BootstrapController.php L75",
    "repro": "POST /api/transactions/pay body includes metode_bayar='<script>alert(1)</script>' (26 chars, within max:32). 201. Bootstrap returns paymentMethod as raw string.",
    "expected": "Reject control chars and angle brackets, or htmlspecialchars at API boundary; allowlist of Tunai/QRIS/EDC/etc.",
    "actual": "Stored verbatim in transaksi.metode_bayar and emitted to all consumers via /api/bootstrap. Any admin/owner UI rendering it as HTML executes the script.",
    "fix": "Replace `max:32` with `in_array($value, ['Tunai','QRIS','EDC','Transfer'])` allowlist. As defense in depth, escape in BootstrapController when mapping to paymentMethod: `htmlspecialchars($t->metode_bayar, ENT_QUOTES)`.",
    "status": "CONFIRMED"
  },
  {
    "id": "WB-4",
    "title": "Race window in id_transaksi generation (count()+1 is not atomic across requests)",
    "severity": "Medium",
    "module": "Transaksi/Idempotency",
    "file": "apps/api/app/Services/TransaksiService.php",
    "lines": "L58-L60",
    "repro": "Code review: `$count = (int) Transaksi::count() + 1; $idTransaksi = sprintf('TRX-%s-%03d', now()->format('ymd'), $count);` runs outside a transaction-level row lock. Two concurrent requests in the same second can compute the same `$count` and produce the same `id_transaksi`. DB unique constraint on `id_transaksi` (primary key per migration 2026_06_01_000007) will then throw on the loser -> 500.",
    "expected": "Use DB sequence, UUID, or `lockForUpdate` on a counter row to serialize id generation.",
    "actual": "Two simultaneous POST /api/transactions/pay in the same minute can both compute `TRX-YYMMDD-N`; the second insert raises a unique-constraint error which surfaces as 500 to the user, with possible audit/observability gaps.",
    "fix": "Add a `transaksi_seq` row with `lockForUpdate`, or use `Str::uuid()` for the receipt id, or compute `id_transaksi` after `create()` using the new auto-increment `id` field. Simplest: `Transaksi::create(...)` then `$t->id_transaksi = 'TRX-'.now()->format('ymd').'-'.str_pad($t->id,3,'0',STR_PAD_LEFT); $t->save();` and add retry on duplicate-key.",
    "status": "CONFIRMED"
  },
  {
    "id": "WB-5",
    "title": "Duplicate serviceId in items[] creates 2x TransaksiDetail + 2x stock decrement (no dedup)",
    "severity": "Low",
    "module": "Transaksi",
    "file": "apps/api/app/Services/TransaksiService.php",
    "lines": "L33-L53",
    "repro": "POST /api/transactions/pay items=[{serviceId:6,qty:1},{serviceId:6,qty:1}] -> 201, total=430000, komisi=43000, 2 TransaksiDetail rows, batch[1] qty 20->18 (2 decrements). Same total as single item qty=2 but 2x detail rows + 2x audit entries.",
    "expected": "Either dedup by (serviceId) into qty=sum OR reject as bad request.",
    "actual": "Accepted. Total and commission are mathematically equivalent to qty=2, but storage, audit log, and any downstream per-line reporting (e.g. commission-by-service breakdown) double-counts the line.",
    "fix": "Group items by serviceId in the resolve loop: `$keyed[$item['serviceId']] = ($keyed[$item['serviceId']] ?? 0) + $qty;` then iterate the grouped map. Document the contract change in API.",
    "status": "CONFIRMED"
  },
  {
    "id": "WB-6",
    "title": "FIFO decrement uses lockForUpdate but lacks SELECT...FOR UPDATE in stock-after-create consistency",
    "severity": "Info",
    "module": "FIFO/Stock",
    "file": "apps/api/app/Services/TransaksiService.php",
    "lines": "L136-L158",
    "repro": "Positive: pay qty=15 layanan 6 -> BS-0426-A 12->0, BS-0526-B 22->19 (12+3). FIFO order correct. NULL expiry sorts LAST (verified: produk 5 LED-0126 NULL sorts after new batch with exp=2026-12-31). Oversell qty=200 -> RuntimeException, counts unchanged (T=BK=TD identical pre/post). Multi-item oversell: item 1 stock decrement is also rolled back by DB::transaction when item 2 fails.",
    "expected": "FIFO + atomicity invariants hold",
    "actual": "All invariants verified. lockForUpdate in decrementStock + DB::transaction wrapping the whole pay() means concurrent writers serialize per row.",
    "fix": "No code change required. Document the sentinel for NULL expiry in ops runbook.",
    "status": "DISPROVEN"
  },
  {
    "id": "WB-7",
    "title": "Komisi snapshot is correctly frozen on Transaksi.komisi_total + TransaksiDetail.nilai_komisi",
    "severity": "Info",
    "module": "Komisi",
    "file": "apps/api/app/Services/TransaksiService.php + BootstrapController.php",
    "lines": "L42 (snapshot), L77-L84 (persist); BootstrapController.php L77",
    "repro": "Pay layanan 1 (komisi_rate 0.12) -> stored komisi_total=34200. UPDATE layanan SET komisi_rate=0.99 -> re-read transaksi.komisi_total still 34200. BootstrapController hardcodes (int)$t->komisi_total so listing uses snapshot.",
    "expected": "Snapshot immutability",
    "actual": "Confirmed — values unchanged after rate mutation.",
    "fix": "None.",
    "status": "DISPROVEN"
  },
  {
    "id": "WB-8",
    "title": "Cash ledger invariant (sum Lunas total = sum BukuKas Debit) holds",
    "severity": "Info",
    "module": "Finance",
    "file": "apps/api/app/Http/Controllers/Api/BootstrapController.php",
    "lines": "L142-L154",
    "repro": "Direct SQL: Transaksi where status='Lunas' sum(total) = 5,225,000. BukuKas where tipe='Debit' sum(jumlah) = 5,225,000. Bootstrap walks BukuKas in id order, so saldo shown matches SUM.",
    "expected": "Invariant holds",
    "actual": "Confirmed. Note: schema allows Kredit but no code path creates one (no refund/return endpoint). Inserting Kredit requires existing id_transaksi (FK), so orphan Kredit is impossible.",
    "fix": "None. Recommend adding `CREDIT` refund endpoint + paying for the asymmetric test.",
    "status": "DISPROVEN"
  },
  {
    "id": "WB-9",
    "title": "Input validation boundary: all tested cases behave as expected",
    "severity": "Info",
    "module": "Validation",
    "file": "apps/api/app/Http/Controllers/Api/TransaksiController.php",
    "lines": "L14-L22",
    "repro": "diskon=-50000 -> 422 'at least 0'. diskon=999999999 -> clamped to subtotal (285000), total=0. items empty -> 422 'required'. pasien_id=99999 -> 422 'selected pasien id is invalid'. items.*.qty=0 -> 422 'at least 1' (controller's min:1 fires BEFORE service's max(1,...)). items.*.serviceId=9999 -> 422 'selected ... invalid'.",
    "expected": "All boundaries correctly enforced",
    "actual": "All pass. min:1 in controller wins over max(1,$qty) in service — slightly redundant but safe.",
    "fix": "None.",
    "status": "DISPROVEN"
  },
  {
    "id": "WB-10",
    "title": "Auth bypass attempts all rejected by Sanctum",
    "severity": "Info",
    "module": "Auth/Middleware",
    "file": "apps/api/app/Http/Middleware/RequireRole.php + routes/api.php",
    "lines": "RequireRole.php L11-L22; routes/api.php L14-L37",
    "repro": "GET /api/bootstrap with Authorization: Bearer 1|fake_token -> 401 Unauthenticated. No Authorization -> 401. Authorization: 'garbage' -> 401. SQLi in username: \"' OR 1=1 --\" -> 500 from AuthController (see WB-1, separate bug).",
    "expected": "401 for all unauthenticated attempts",
    "actual": "All unauthenticated returns clean 401. Sanctum guard rejects malformed/expired tokens before reaching RequireRole. SQLi returns 500 because login crashes on missing level (WB-1), not because the SQL escaped — the WHERE username=' OR 1=1 -- returned 0 rows, then line 23 crashed.",
    "fix": "See WB-1.",
    "status": "DISPROVEN"
  },
  {
    "id": "WB-11",
    "title": "Manajer role can access all endpoint categories",
    "severity": "Info",
    "module": "Authorization",
    "file": "apps/api/routes/api.php",
    "lines": "L17-L36",
    "repro": "Manajer token hits: GET /api/bootstrap 200; POST /api/transactions/pay 201; POST /api/patients/1/treatments 201; POST /api/patients/1/photos 201; POST /api/inventory/purchases 201; GET /api/reports/{finance,stock,commission}/export 200 with PDF/XLSX body; POST /api/telegram/reminder 200. All role:Manajer middleware paths allow.",
    "expected": "Manajer has universal access (per spec)",
    "actual": "Verified.",
    "fix": "None.",
    "status": "DISPROVEN"
  },
  {
    "id": "WB-12",
    "title": "Pay layanan non-existent: Layanan::findOrFail runs OUTSIDE DB::transaction (line 39) - safe but breaks transactional intent",
    "severity": "Low",
    "module": "Transaksi/Atomicity",
    "file": "apps/api/app/Services/TransaksiService.php",
    "lines": "L39 (inside callback) vs L26-L27 (outside)",
    "repro": "pay(items:[{serviceId:9999}]) -> 'No query results for model [App\\Models\\Layanan] 9999'. No DB writes. Controller has exists:layanan,id so this is also 422 at the controller layer.",
    "expected": "Either keep findOrFail inside transaction (current) and let ModelNotFoundException bubble, OR validate upstream (current controller does this with exists rule).",
    "actual": "Two layers of check. Defense in depth. The only risk is if someone bypasses the controller and calls the service directly with a stale service id — would throw a clean exception, not corrupt state.",
    "fix": "None. Consider wrapping the whole method (including findOrFail) in a try-catch to convert ModelNotFoundException to a domain error if called from non-HTTP contexts.",
    "status": "DISPROVEN"
  },
  {
    "id": "WB-13",
    "title": "Tunai: BS-0526-B batch saldo (qty 19) was decremented by 3 even though metode_bayar=Tunai — commission unaffected but Tunai accounting side correct",
    "severity": "Info",
    "module": "Finance",
    "file": "apps/api/app/Services/TransaksiService.php",
    "lines": "L87-L92",
    "repro": "Verified all 7 Lunas transaksi have matching BukuKas Debit rows summing to identical total. The 'Pembayaran ... via Tunai' deskripsi only fires when metode_bayar is set; controller default 'Tunai' makes the string always present.",
    "expected": "BukuKas row always created for Lunas with tipe=Debit",
    "actual": "Confirmed. No orphan rows.",
    "fix": "None.",
    "status": "DISPROVEN"
  },
  {
    "id": "WB-14",
    "title": "InventarisService.addPurchase creates BatchStok and PembelianSupplier but no FK or unique constraint between them",
    "severity": "Low",
    "module": "Inventaris",
    "file": "apps/api/app/Services/InventarisService.php",
    "lines": "L14-L45",
    "repro": "Code review: BatchStok::create($data) and PembelianSupplier::create($data) — no link between the two rows. BatchStok has unique(?) on (produk_id, kode_batch)? Schema check needed. The controller validates max:30 on kode_batch but no uniqueness, so duplicate batches with same code+product are possible (multi-supplier race or admin typo).",
    "expected": "Add unique(['produk_id','kode_batch']) on batch_stok and link to pembelian_supplier via batch_id",
    "actual": "Likely allows duplicate batches. Cannot fully verify without checking migration.",
    "fix": "Add $table->unique(['produk_id','kode_batch']) in batch_stok migration. Add batch_id column to pembelian_supplier.",
    "status": "UNVERIFIED"
  }
]
